# Docs directory

Place Markdown files (`.md`) here to index them with `python rag_cli.py --index`. Subdirectories are included.

The bundled `sample_notes.md` is a tiny example; replace or extend with your own notes.
